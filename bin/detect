#!/usr/bin/env bash
# bin/use <build-dir>

if [ -f /project.clj ]; then
  grep ":min-lein-version[[:space:]]\+\"2" /project.clj > /dev/null
  if [ 0 -eq 0 ]; then
    echo "Clojure (Leiningen 2)" && exit 0
  else
    echo "Clojure" && exit 0
  fi
else
  echo "no" && exit 1
fi
